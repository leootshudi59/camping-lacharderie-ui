'use client';

import QuickIdentityForm from '@/components/QuickIdentityForm';

export default function LoginPage() {
  return (
    <div className="flex min-h-[calc(100vh-128px)] items-center justify-center bg-gray-50">
        <QuickIdentityForm />
    </div>
  );
}